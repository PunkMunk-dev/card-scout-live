

## Plan: Enlarge Logo Icon on Hover

Add a hover scale transform to the `OmniIcon` component in `src/components/branding/OmniIcon.tsx`.

Add a CSS transition + hover scale to the `<img>` element: `transition: transform 200ms` and `hover:scale-110` (or equivalent inline style). This will apply everywhere the icon is used (header, sidebar, etc.).

