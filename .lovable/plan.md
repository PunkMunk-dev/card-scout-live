

# Hide Input Characters in Access Gate

## Change
In `src/components/auth/AccessGate.tsx`, change the input `type` from `"text"` to `"password"` so characters are masked as dots while typing.

## File: `src/components/auth/AccessGate.tsx`
- Line ~96: Change `type="text"` to `type="password"`

This is a one-line change. No other files affected.

